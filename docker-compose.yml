name: wms-monorepo

services:
  api-gateway:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/api-gateway:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8000:8000"

  identity-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/identity-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8001:8001"

  customer-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/customer-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8002:8002"

  product-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/product-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8003:8003"

  warehouse-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/warehouse-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8004:8004"

  inventory-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/inventory-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8005:8005"

  documents-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/documents-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8006:8006"

  audit-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/audit-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8007:8007"

  reporting-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/reporting-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8008:8008"

  ai-service:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./Services/ai-service:/workspace
    command: ["bash", "-lc", "python -V && echo 'TODO: add Dockerfile / deps install' && sleep infinity"]
    ports:
      - "8009:8009"

